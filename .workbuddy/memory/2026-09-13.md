# 2026-09-13

- 应用户质询「粒度够不够 1:1 复刻」，实测补齐附录 A-E：
  A 端到端 Runbook（命令级，标 [BG] 后台步骤）、B 中间文件 Schema（实测
  jlpt_books 行字段 / gen_words / gen_out / sentences_master / audio_manifest /
  翻译批次两种形状）、C 脚本 CLI 速查（逐脚本核对 argparse/sys.argv）、
  D BepInEx 插件构建（NETFX csc v4.0.30319, /langversion:5, BookProfiles.cs
  共享, steam appid 1981560）、E 诚实边界（LLM 精翻 prompt 未落盘 / 词源人工
  筛选 / 子代理编排策略 / 安装器需以脚本为蓝本）。
- 实测结论：gen_words_NN 每块 400 词 × 4 批(100词)；gen_pipeline 默认子命令
  summary；gen_helper 有 plan/audit/merge；gen_dispatch 有 status/claim/batch/done。
  结构：游戏机制契约（MyBook.es3 ES3 格式、槽位/署名约束、xlsx 无表头契约、
  灌库 pron+sentence2、音频 md5 命名、wcp_paths 多副本解析）→ 六阶段数据流水线
  （词源→分层翻译→校验→例句子代理契约→音频→落库）→ 新语言适配清单（不变/必换）
  → 运维坑 → 交付物基线。内容源：wcp_wordbooks/README.md、work/*_SPEC.md、
  关键脚本 docstring（patch_local_db / wcp_paths / write_mybook / make_import_files /
  gen_audio / gen_sentence_audio / gen_pipeline / apply_sentences / merge_translations）、
  mod_*.cs 注释。
