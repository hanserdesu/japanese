# 2026-09-10 工作日志

## 1. 项目接手：定位断点

`D:/Japanese` 无 git、无记忆文件、无工作日志，靠文件 mtime 和工具脚本自陈重建状态。

**任务是 WCP 日语词书的「例句生成」流水线**，09-07 起、09-08 02:35 中断。

实测战况（`tools/gen_pipeline.py summary`）：
- 总 15812 词 / 159 批；已达标 5100 词（51 批，全部 PASS）
- 待完成 **10712 词 / 108 批 / 25329 条例句**
- 断点：最后产出 `work/gen_out_12_2.json`；从 12_3 起全未生成
- 已存在但不达标的批次 = 0 → **纯产能中断，非质量事故**

翻译侧（`tools/check_sentence_progress.py`）：10251 句已 100% 齐。

**证据边界：** 原 51 批如何产出在仓库无留痕（`tools/` 无对应脚本，`agent_worklog.md` 停在 09-06），无法复现原通道，只能按 `gen_pipeline.py` 校验契约产出。

## 2. 完成 13_0

`work/gen_out_13_0.json` — 100 词 / 231 句，首次 PASS。
踩坑：`化する` 报「词形未出现(活用?)」—— 例句只写「化した」不行，必须让原形「化する」出现。

## 3. git 化项目

- 之前**无任何版本控制**（09-06~09-08 全部工作无历史）
- 新建 `.gitignore`：只版本化「源码 + 自有数据 + 文档」
  - 忽略体积大头：JMDict 113MB、kanjidic2 15MB、游戏库备份 43MB、
    `work/decompiled/` 12MB、`full_il_dump.txt` 50MB、所有 mp3/wav、
    `output/` 下可重建产物
  - 版本化：45 个 tools 脚本、data/ 自有数据、work/ 例句产物、README.md
- 首次提交 `4fcb1e1`：379 文件，工作区 297MB → **仓库 7.7MB**
- `core.autocrlf false`（避免 CRLF 污染日文文本）

## 4. 写入 Hindsight

三条文档：项目总览与游戏导入契约 / 例句流水线进度与断点 / 仓库状态与 gitignore 策略。

## 5. 统一 3 条标准 + 工具链升级 + 并行受挫（晚间续）

- 用户确认：**每词统一 3 例句**（弃 need 2/3 混用）、**复用已有进度**、方案 A（硬做+deepseekv4.1flash 并行子代理）
- `gen_pipeline.py`：`MIN_EXAMPLES=3`（`GEN_MIN` 可回退）
- 新增 `gen_helper.py`（plan/merge/audit）、`gen_dispatch.py`（claim/status/batch/done）、`AGENT_SPEC.md`
- 13_0 首批 79 词回补第 3 句 → `patch_13_0.json` merge → PASS，验证回补流程
- 首个子代理跑通 13_1~13_3；放量 6 并行时 **429 限流**（重置 09-11 17:39 UTC+8），改顺序手工
- 14_0/14_1 是 429 子代理死前产出，直接复用 PASS

## 6. 进度（本日终）

- PASS 批次：13_0~13_3、14_0~14_3、15_0~15_3、16_0~16_3 = **16 批 / 1600 词**
- 全部通过 `check-one` 七条硬校验；无一带着 FAIL 入库
- git 提交：`4fcb1e1`（初始化）→ `af2acfc`（13 批 + 工具链）→ `5b2d7a2`（block 16）
- 剩余：**145 批**（NEW 94 + PATCH 51，PATCH=旧 2 句批次回补第 3 句）
- 自查陷阱记录：ja 混英文（crew/thirty/suddenly）、ja 混简体字（选/绩/晚/灾）、
  zh 混假名（は/が/れる/けれども）、活用词丢原形（遮った≠遮る）、
  中文词混入（搜救/僵局/今晚）、数字量词错配（二本速い→二駅速い）、
  搭配错误（全身を尽くす→全力を尽くす）
- **生产节奏**：每批约需词表提取 → 写文件 → 自查修错 → check-one，一次 PASS 率约 30%，
  错误集中在前述 7 类陷阱
- 15_1 曾有 3 处编辑残留注释行 + 1 处「成绩」简体字，均修复后 PASS
- 自查陷阱记录：ja 混英文（crew）、ja 混简体字（选/绩）、zh 混假名（は/が/れる）、
  活用词丢原形（遮った≠遮る）、数字量词错配（二本速い→二駅速い）

## 待办

- [ ] 继续 16_1 起顺序生产（每批 100 词 × 3 句）
- [ ] 09-11 17:39 后可恢复并行子代理放量
- [ ] PATCH 类 51 批回补（用 gen_helper.py merge 流程）
