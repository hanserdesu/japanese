# 2026-09-14 工作日志

## 复查并重写 IMPLEMENTATION.md（复刻可交付性审查）

**任务**：判断现行复刻文档能否让新语言"完美复刻"，审阅粒度与细节，然后优化。

**做了什么**（全部基于磁盘/DB/源码实测，不采信文档自述）：
1. 核对文档引用的 100 个文件名 → 全部可解析（缺失项都是合法运行时物件：游戏 DLL、
   `MyBook.es3`、`gen_out_NN_Y` 之类的模式串）。
2. 逐个核对 CLI 参数（`--reuse-audio` / `--skip-audio` / `--force` / `GEN_MIN` /
   `--limit` / `--retry-failed` / `build_grammar_book --patch`）→ 全部属实。
3. 逐条核对数字 → **全部过期**（详见下方结论区）。
4. 读 `build.cmd` / 安装器 / 三个插件源码 → 发现 6 处文档与实现不符。
5. 用 `D:/French/AUDIT-2026-09-14.md` 当"复刻效果"的实证，反向定位文档缺口。
6. **复刻 `ExtractJa` 对全量 57,687 条例句跑端到端契约断言 → 发现真缺陷**。

**产出**：
- 重写 `D:/Japanese/IMPLEMENTATION.md`（新增 §1 项目骨架 / §3 多语言共存目录规约 /
  §8 契约→可执行检查 / §4 支线流水线；修正 6 处错误表述；数字换成实测值+复算命令）
- 新增 `D:/Japanese/wcp_wordbooks/AUDIT-2026-09-14.md`（核对过程与证据）

**核心发现（必须记住）**：
- **日语侧现存 386 条例句「▶ 按钮在但点了没声音」**：`ExtractJa` 取**最后一个** `（`
  切尾，中文译文含全角括号时剥离结果 ≠ 原文 → md5 mismatch。386 = 译文含全角括号的全部集合。
  三处检查都不覆盖：`gen_pipeline` 只拦假名、`verify_all` 只数条数、音频生成只报写盘成功。
  法语侧已在 `gen_pipeline_fr.py` 加了"译文禁括号"校验，**等于法语无意中修了日语没修的问题**。
- **`vocabulary/` 与 `sentence_audio/` 是多语言共享目录**，目录计数不能当单语言验收基线。
  拉丁语系（法语 1,638/8,116 同形词）共享 `vocabulary/` 会让英/日词书串音 → 必须私有化。
- 目录规约：共享 `vocabulary/`、`sentence_audio/`；私有 `<lang>_word_audio/`、
  `<lang>_db_payload/`、`<lang>mod_backups/`。

**本次未做**：386 条的修复（涉及写游戏库，且要重算音频文件名）；多语言实机切换矩阵。

**环境观察**：`vocabulary/` 计数在审计期间 19,141 → 19,175 → 19,196 持续变化，
`LocalLow\WCP\wcp\` 有其它语言在写 → 存在第二方并发进程，读数取瞬时值。

---

## 统一接入架构设计（资源逻辑 ↔ 接入逻辑解耦）

**任务**：用户问能否让所有语言复用同一套接入逻辑接入游戏，语言资源互相隔离、
只有选中词书才激活，同时接入逻辑只有一套。

**结论**：可行，方向对，但做不到"纯数据语言包"。

**新增产出**：`D:/Japanese/ARCHITECTURE-UNIFIED.md`（Host / Pack / Strategy 三层设计）
— §1 现状实测耦合点、§2 三层架构 + manifest schema + `ILanguageStrategy` 接口、
§3 三处物理无法解耦、§4 四阶段迁移路径、§5 九条可执行验收、§6 否决方案。

**实测数据（本次新测）**：
- fork 差异量（`diff --strip-trailing-cr`）：词表 2339 行→JP/FR 差 2084（复用 11%）、
  JP/RU 差 1811；例句 923→732/527；**书名 693→26（复用 96%）**。
  → 12 个 DLL ≈ 11,700 行，真实独立逻辑不足 1,500 行。
- 五处共享资源，三种互不一致的命名空间策略：
  单词音频 JA 用游戏 `vocabulary\` / FR 私有 `fr_word_audio\` / RU 私有 + 回退；
  例句音频三语言全共享 `sentence_audio\`，靠 Extract 谓词互斥（假名/拉丁/西里尔）；
  仅 `*_db_payload` 已命名空间化。
- `BookProfiles.cs` 副本漂移：JA 单份共享（build.cmd 引 `..\mod_book_name\`），
  FR/RU 各 3 份拷贝。
- 槽位预算：`_slotProfiles[4]` + 硬编码「自定义词书一~四」→ **4 槽 = 最多 4 语言**。
  `日语词库(猫条版).xlsx` 7,922 词 = 1 本书 = 1 槽 = 1 指纹（与
  `BookProfile("catbar-jlpt-complete", …, 7922, "6d7a51c0…")` 精确对应）；
  `JLPT_N1/N2/N3/N5N4/IT用语` 是合并前的源片段，不单独占槽。

**三条诚实边界**（写进设计文档 §3）：
1. 策略不可纯数据化：提取器谓词 / 题干选项变形 / 音频回退形式 / 自愈探针，
   至少 4 类是实现行为 → 每语言留 ~150 行 `ILanguageStrategy`。
2. 运行态无法并存：`allTestWordsS10_Para`、`S8needToLearnWordList_Para`、
   `exmplesentences` 都是全局单例 → 只能串行 + 接管-还原；
   ES3 键要从全局 `JpWL_*` 改为 `<lang>_` 前缀。
3. DB 单文件单主键（`word`）→ 必须停止写入，改宿主自服务（`HealExampleDatabase` 退役）。

**未做**：任何实施改动。设计稿状态。

