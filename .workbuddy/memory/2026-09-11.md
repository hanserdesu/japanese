# 2026-09-11

## 例句生产收尾（"继续任务"）

- 起点: gen_pipeline summary = 达标词 4000/15812, 剩 119 批（27 PATCH + 92 NEW）
- 执行: 7 波并行子代理（每波 3-4 个，契约 = work/AGENT_SPEC.md + work/PATCH_SPEC.md）
  - PATCH 06-12 共 27 批补齐（每波先自己跑 summary 复核，不信代理自述）
  - NEW 12_3, 17-39 共 92 批新建，全部 check-one PASS
- 终态: **15,812/15,812 词达标，每词恰好 3 条，剩余批次 0**
- 旧例句翻译: tr_chunk 17 块中 11 块（5,771 句）缺翻译 → 3 个子代理补完 →
  9,071/9,071 全覆盖
- 落库: apply_sentences.py 跑两次（第一次 2min 前台超时被 SIGTERM，单事务未
  commit 无损；改后台 -u 重跑）。最终: UPDATE 10251（旧句补中文），INSERT 0
  （首轮后台运行已完成插入），master 15,812 词 缺翻译 0 <3条 0
- **全量核验**: wcpFullEng.db sentence2 逐词全查（GROUP BY word 对比 master），
  15,812 词全部 ≥3 条。お産 抽检 3 句格式正确带中文。

## 本会话环境坑（后续会话注意）

- PowerShell stdout 捕获**彻底坏了**（连 Write-Output 都返回空）——只能用 bash
- bash PATH 缺 coreutils（dirname/head/ls 全挂），python 用绝对路径
  `C:/Users/hanserdesu/.workbuddy/binaries/python/versions/3.13.12/python.exe`
- apply_sentences.py 全量落库要 6+ 分钟，**必须后台跑**（-u 无缓冲），前台 2min 超时
- 后台 Agent 偶发重复实例（多个代理报「文件已被并行进程写入」，甚至越界做了
  未分配的 29_0/37_0/37_1）。无损——check_slice 是统一校验收口，谁写的都得过检。
  抽查越界产物质量合格。
- README.md 已追加「例句全覆盖 (2026-09-11 完成)」一节
